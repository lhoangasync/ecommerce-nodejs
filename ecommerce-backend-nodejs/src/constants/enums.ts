export enum UserVerifyStatus {
  UNVERIFIED,
  VERIFIED,
  BANNED
}

export enum UserRoles {
  USER,
  ADMIN
}

export enum TokenType {
  ACCESSTOKEN,
  REFRESHTOKEN,
  FORGOTPASSWORDTOKEN,
  EMAILVERIFYTOKEN
}
